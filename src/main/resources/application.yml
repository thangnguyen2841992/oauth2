server:
  port: 8081
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/nihongo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: thuthuyda1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  idp:
    url: http://localhost:8180
    client-id: japanese_app
    # company
    client-secret: KL3QoB3145oUkIfyNfnbZzBDd3vY8JcD
    #home
#    client-secret: DoJs3BI1BiKCzaf9rRWvxpVPsilWoKcU
    realm: nihongo

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/nihongo

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      acks: all
      enable-idempotence: true
      retries: 5
      max-in-flight-requests-per-connection: 1
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

api:
  key: thangdeptrai
logging:
  level:
    org.springframework.cloud.gateway: DEBUG